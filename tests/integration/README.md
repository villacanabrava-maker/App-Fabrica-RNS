# Testes de integração

Vazio de propósito no Sprint 1.1. Testes de integração de verdade (rotas de comando contra banco de teste) chegam no Sprint 1.3 (`packages/domain`) e Sprint 1.4 (Orchestrator), conforme `docs/08-PLANO-DE-IMPLEMENTACAO/02-FASE-1-APP-FUNCIONAL.md`.

`pnpm test:integration` roda com `--passWithNoTests` até lá — não finge que há testes que não existem.
